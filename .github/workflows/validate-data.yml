name: Data Quality Check

on:
  schedule:
    - cron: '0 * * * *'  # TODO: Choose your frequency (15min? hourly? daily?)
  push:
    branches: [main]
    paths:
      - 'week3/data/**'
      - 'week3/validation/**'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install pandas pyarrow numpy

      - name: Run data quality validation
        id: validate
        run: |
          cd week3
          # TODO: Implement this
          # - Load and split the corrupted parquet (pre/post Jan 16)
          # - Run your validation checks
          # - Report findings
          # - Exit with error if validation fails
          python3 -m validation.check_data_quality

      - name: Block deployment if validation fails
        if: failure()
        run: |
          echo "Data validation FAILED"
          echo "Check logs above for issues"
          exit 1

      - name: Validation passed
        if: success()
        run: echo "Data quality validation passed"
